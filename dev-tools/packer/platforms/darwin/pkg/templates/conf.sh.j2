BEAT={{.beat_name}}
VERSION={{.version}}{{.snapshot}}
ARCH={{.osx_arch}}
VENDOR="{{.beat_vendor}}"
LICENSE="{{.beat_license}}"
IDENTIFIER="{{.identifier_base}}.{{.beat_name}}"
INSTALL_PATH="{{.install_path}}"
