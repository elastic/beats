# Source: network_traffic/data_stream/flow/agent/stream/flow.yml.hbs
type: flow
timeout: 30s
period: -1s
fields_under_root: true
fields:
  _conf:
    geoip_enrich: false
    map_to_ecs: true
processors:
  - add_host_metadata: ~
interface:
  device: default_route
data_stream:
  dataset: network_traffic.flow
  type: logs
