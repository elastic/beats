# Source: network_traffic/data_stream/http/agent/stream/http.yml.hbs
type: http
ports:
  - 80
fields_under_root: true
fields:
  _conf:
    geoip_enrich: false
    map_to_ecs: true
processors:
  - add_host_metadata: ~
interface:
  device: default_route
procs:
  enabled: true
data_stream:
  dataset: network_traffic.http
  type: logs
