# Source: network_traffic/data_stream/sip/agent/stream/sip.yml.hbs
type: sip
ports:
  - 5060
fields_under_root: true
fields:
  _conf:
    geoip_enrich: false
    map_to_ecs: true
processors:
  - add_host_metadata: ~
interface:
  device: default_route
procs:
  enabled: true
data_stream:
  dataset: network_traffic.sip
  type: logs
