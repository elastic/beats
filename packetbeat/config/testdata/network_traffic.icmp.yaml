# Source: network_traffic/data_stream/icmp/agent/stream/icmp.yml.hbs
type: icmp
fields_under_root: true
fields:
  _conf:
    geoip_enrich: false
    map_to_ecs: true
processors:
  - add_host_metadata: ~
interface:
  device: default_route
procs:
  enabled: true
data_stream:
  dataset: network_traffic.icmp
  type: logs
