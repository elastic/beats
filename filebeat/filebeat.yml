## TODO: restore before merging and add this config to the PR when explaining how
## to test the PR

filebeat.config.inputs:
  enabled: true
  path: inputs.d/*.yml
  reload.enabled: true
  reload.period: 5s

output.discard.enabled: true
logging:
  level: debug
  metrics:
    enabled: false
