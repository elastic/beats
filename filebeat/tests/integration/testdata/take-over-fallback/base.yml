filebeat.config.inputs:
  path: {{.inputsDir}}/*.yml
  reload.enabled: true
  reload.period: 1s

path.home: {{.homePath}}

output.file:
  path: ${path.home}
  filename: "output-file"
  rotate_on_startup: false

logging.level: debug
