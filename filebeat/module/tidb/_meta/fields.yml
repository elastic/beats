# The module level fields.yml contains descriptions for the module-level fields.
# Please review and update the title and the descriptions in this file.
# The title is used as a title in the docs, so it’s best to capitalize it.
- key: tidb
  title: "TiDB"
  release: ga
  description: >
    tidb Module
  fields:
    - name: tidb
      type: group
      description: >
      fields:
        - name: host
          type: alias
          path: source.domain
          migration: true
        - name: ip
          type: alias
          path: source.ip
          migration: true
