module_version: 1.0

var:
  - name: deployment
    # baremetal or container
    default: baremetal
  - name: paths
    default:
      # the default path of tiup
      - /tidb-deploy/tikv-20160/log/*.log

ingest_pipeline: ingest/pipeline.json
input: config/{{.deployment}}.yml
