linters-settings:
  maligned:
    suggest-new: true

linters:
  disable:
    - structcheck
    - errcheck
      #- typecheck

issues:
  exclude:
    - "`?encodeFixed(32|64)Pb`? is unused"
    - "`?_Ctype[^ `]*`? is unused"
    - "`?_cgo[^ `]*`? is unused"
    - "SA4000"
    - "Example[^ ]* refers to unknown"

# vim: set et shiftwidth=2:
