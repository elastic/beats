default: true
input:
  module: prometheus
  metricset: collector
  defaults:
    metrics_path: /metrics
    # Set these to override openmetrics.yml configuration settings
    #metrics_filters:
    #  include: []
    #  exclude: []
