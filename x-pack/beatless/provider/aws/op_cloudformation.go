package aws

// cf := cloudformation.New(awsCfg)
// create := &cloudformation.CreateStackInput{
// 	ClientRequestToken: aws.String(string(time.Now().Unix())),
// 	OnFailure:          cloudformation.OnFailureRollback,
// 	StackName:          aws.String("beatless-" + c.config.Name),
// }

// req := cf.CreateStackRequest(create)
// resp, err := req.Send()
// if err != nil {
// 	c.log.Errorf("cloud not send date to aws, resp: %v", resp)
// 	return err
// }
