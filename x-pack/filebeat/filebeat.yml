###################### Filebeat Configuration Example #########################

# This file is an example configuration file highlighting only the most common
# options. The filebeat.reference.yml file from the same directory contains all the
# supported options with more comments. You can use it as a reference.
#
# You can find the full configuration reference here:
# https://www.elastic.co/guide/en/beats/filebeat/index.html

# For more available modules and options, please see the filebeat.reference.yml sample
# configuration file.

# ============================== Filebeat inputs ===============================

filebeat.inputs:

- type: cel
  enabled: false
  auth.digest:
    user: "fpczxkxc"
    password: "ceb46652-b9a3-4889-92a1-6633e0dda890"
  resource.url: https://cloud.mongodb.com/api/atlas/v1.0/groups/646f4379c47da356740d14ad/clusters/atlas-ccx4uc-shard-00-00.q5ljb.mongodb.net/logs/mongodb-audit-log.gz?endDate=1700956800&startDate=1700955700
  state:
    query: "/measurements?granularity=PT5M&period=PT5m"
    want_more: false
    next_url: ""
  program: |
    (
      string(bytes(get(state.url).Body).mime('application/gzip')).split("\n").map(body, {
          "events": body.decode_json()
        }
      )
    )
  # .mime('application/gzip').decode_json()

- type: cel
  enabled: false
  redact:
    fields:
      - path
    delete: true
  interval: 5s
  auth.oauth2:
    client.id: 3MVG9n_HvETGhr3BNn.eD22p.G8AhQ.F8wFg9_pSgbUKvyJZlyzR9f1W6llXfQvfjmcVYc.4LSBPjd8u9DpQH
    client.secret: B7E6F2FEE72BFDF2C5B9860D0DA1A35D4E5F86694FA35A77E90E056B1564DD19
    token_url: https://crestdatasystems16-dev-ed.develop.my.salesforce.com/services/oauth2/token
    user: ali@crest.sandbox
    password: Letsgothere#$1234
  resource.url: "https://crestdatasystems16-dev-ed.develop.my.salesforce.com"
  # resource.url: "http://localhost:8010"
  state:
    want_more: false
    default: "SELECT Id,CreatedDate,LogDate,LogFile FROM EventLogFile WHERE Interval = 'Hourly' AND EventType = 'Login' ORDER BY CreatedDate ASC NULLS FIRST"
    query: "SELECT Id,CreatedDate,LogDate,LogFile FROM EventLogFile WHERE Interval = 'Hourly' AND EventType = 'Login' AND LogDate > [[.cursor.last]] ORDER BY CreatedDate ASC NULLS FIRST"
    path: "/services/data/v52.0/query"
    initial_interval: 24h
    url: "https://crestdatasystems16-dev-ed.develop.my.salesforce.com"
  program: |
    bytes(get(string(state.url + state.path).parse_url().with({"RawQuery":({"q":[(!has(state.cursor) || !has(state.cursor.last) != ''? state.default : state.query)]}).format_query()}).format_url()).Body)
    .decode_json()
    .as(body,body.records)
    .map(e, e.with({
        "csv": bytes(get(state.url + e.LogFile).Body).mime("text/csv; header=present"),
        "e": e,
    }).collate("csv").map(list, {
        "csv": list,
        "storeDate": {
            "createdDate": e.CreatedDate
        }
    })).flatten().as(res, {
        "events": res.collate("csv"),
        "cursor": res.collate('storeDate.createdDate').as(t, {
            "first": t.min(),
            "last": t.max(),
        })
    })

# Username:- salesforcetest081@gmail.com
# Password:- Salesforceinstance786

- type: salesforce
  enabled: true
  version: 56
  auth.oauth2:
    enabled: false
    client.id: 3MVG9pRzvMkjMb6nzW6iSPJU2rxCryzVeeo7KXuRlI1uqDsJhxdi76lQ5Eks2KSfr1o8D1PoMoGQCPPILnnkB
    client.secret: C497A710C21497FB0A1E68B8DAF78DCFBE455C41BFBE076E85B9B37553D6CAAF
    token_url: https://devtestin-dev-ed.develop.my.salesforce.com/services/oauth2/token
    user: salesforceinstance@devtest.in
    password: testinginstance4salesforce
  auth.jwt:
    enabled: true
    client.id: 3MVG9pRzvMkjMb6nzW6iSPJU2r_G5FX9VkEC9kY.5sHaN1HrUSJzRy3dlofAYPyxJo_aEetg0zwp3wmnKJjyY
    client.username: salesforceinstance@devtest.in
    client.key_path: server_client.key
    url: https://login.salesforce.com
  url: "https://devtestin-dev-ed.develop.my.salesforce.com"
  data_collection_method:
    event_log_file:
      interval: 1h
      enabled: true
      query:
        default: "SELECT Id,CreatedDate,LogDate,LogFile FROM EventLogFile WHERE EventType = 'Login' ORDER BY CreatedDate ASC NULLS FIRST"
        value: "SELECT Id,CreatedDate,LogDate,LogFile FROM EventLogFile WHERE EventType = 'Login' AND CreatedDate > [[ .cursor.logdate ]] ORDER BY CreatedDate ASC NULLS FIRST"
      cursor:
        field: "CreatedDate"
    object:
      interval: 5m
      enabled: true
      query:
        default: "SELECT FIELDS(STANDARD) FROM LoginEvent"
        value: "SELECT FIELDS(STANDARD) FROM LoginEvent WHERE EventDate > [[ .cursor.logdate ]]"      
      cursor:
        field: "EventDate"

- type: httpjson
  id: 100
  enabled: false
  interval: 1h
  request.method: GET
  auth.oauth2:
    enabled: true
    client.id: 3MVG9pRzvMkjMb6nzW6iSPJU2rxCryzVeeo7KXuRlI1uqDsJhxdi76lQ5Eks2KSfr1o8D1PoMoGQCPPILnnkB
    client.secret: C497A710C21497FB0A1E68B8DAF78DCFBE455C41BFBE076E85B9B37553D6CAAF
    token_url: https://devtestin-dev-ed.develop.my.salesforce.com/services/oauth2/token
    user: salesforceinstance@devtest.in
    password: testinginstance4salesforce
  request.url: https://devtestin-dev-ed.develop.my.salesforce.com/services/data/v52.0/query?q=login+rest
  request.transforms:
    - set:
        target: url.params.q
        value: "SELECT Id,CreatedDate,LogDate,LogFile FROM EventLogFile WHERE LogDate > [[.cursor.last_published_apex]] AND EventType = 'Login' ORDER BY LogDate ASC NULLS FIRST"
        default: "SELECT Id,CreatedDate,LogDate,LogFile FROM EventLogFile WHERE EventType = 'Login' ORDER BY LogDate ASC NULLS FIRST"
  response.pagination:
    - set:
        target: url.value
        value: '[[if (ne .last_response.body.done true)]]https://devtestin-dev-ed.develop.my.salesforce.com[[.last_response.body.nextRecordsUrl]][[end]]'
        fail_on_template_error: true
  chain:
    - step:
        request.url: https://devtestin-dev-ed.develop.my.salesforce.com/services/data/v52.0/sobjects/EventLogFile/$.records[:].Id/LogFile
        request.method: GET
        replace: $.records[:].Id
  cursor:
    last_published_apex:
      value: '[[(formatDate ((parseDate .last_event.TIMESTAMP_DERIVED "RFC3339").Add (parseDuration "-1h")))]]'

# Each - is an input. Most options can be set at the input level, so
# you can use different inputs for various configurations.
# Below are the input-specific configurations.

# filestream is an input for collecting log messages from files.
- type: filestream

  # Unique ID among all inputs, an ID is required.
  id: my-filestream-id

  # Change to true to enable this input configuration.
  enabled: false

  # Paths that should be crawled and fetched. Glob based paths.
  paths:
    - /var/log/*.log
    #- c:\programdata\elasticsearch\logs\*

  # Exclude lines. A list of regular expressions to match. It drops the lines that are
  # matching any regular expression from the list.
  # Line filtering happens after the parsers pipeline. If you would like to filter lines
  # before parsers, use include_message parser.
  #exclude_lines: ['^DBG']

  # Include lines. A list of regular expressions to match. It exports the lines that are
  # matching any regular expression from the list.
  # Line filtering happens after the parsers pipeline. If you would like to filter lines
  # before parsers, use include_message parser.
  #include_lines: ['^ERR', '^WARN']

  # Exclude files. A list of regular expressions to match. Filebeat drops the files that
  # are matching any regular expression from the list. By default, no files are dropped.
  #prospector.scanner.exclude_files: ['.gz$']

  # Optional additional fields. These fields can be freely picked
  # to add additional information to the crawled log files for filtering
  #fields:
  #  level: debug
  #  review: 1

# ============================== Filebeat modules ==============================

filebeat.config.modules:
  # Glob pattern for configuration loading
  path: ${path.config}/modules.d/*.yml

  # Set to true to enable config reloading
  reload.enabled: false

  # Period on which files under path should be checked for changes
  #reload.period: 10s

# ======================= Elasticsearch template setting =======================

setup.template.settings:
  index.number_of_shards: 1
  #index.codec: best_compression
  #_source.enabled: false


# ================================== General ===================================

# The name of the shipper that publishes the network data. It can be used to group
# all the transactions sent by a single shipper in the web interface.
#name:

# The tags of the shipper are included in their field with each
# transaction published.
#tags: ["service-X", "web-tier"]

# Optional fields that you can specify to add additional information to the
# output.
#fields:
#  env: staging

# ================================= Dashboards =================================
# These settings control loading the sample dashboards to the Kibana index. Loading
# the dashboards is disabled by default and can be enabled either by setting the
# options here or by using the `setup` command.
#setup.dashboards.enabled: false

# The URL from where to download the dashboard archive. By default, this URL
# has a value that is computed based on the Beat name and version. For released
# versions, this URL points to the dashboard archive on the artifacts.elastic.co
# website.
#setup.dashboards.url:

# =================================== Kibana ===================================

# Starting with Beats version 6.0.0, the dashboards are loaded via the Kibana API.
# This requires a Kibana endpoint configuration.
setup.kibana:

  # Kibana Host
  # Scheme and port can be left out and will be set to the default (http and 5601)
  # In case you specify and additional path, the scheme is required: http://localhost:5601/path
  # IPv6 addresses should always be defined as: https://[2001:db8::1]:5601
  #host: "localhost:5601"

  # Kibana Space ID
  # ID of the Kibana Space into which the dashboards should be loaded. By default,
  # the Default Space will be used.
  #space.id:

# =============================== Elastic Cloud ================================

# These settings simplify using Filebeat with the Elastic Cloud (https://cloud.elastic.co/).

# The cloud.id setting overwrites the `output.elasticsearch.hosts` and
# `setup.kibana.host` options.
# You can find the `cloud.id` in the Elastic Cloud web UI.
#cloud.id:

# The cloud.auth setting overwrites the `output.elasticsearch.username` and
# `output.elasticsearch.password` settings. The format is `<user>:<pass>`.
#cloud.auth:

# ================================== Outputs ===================================

# Configure what output to use when sending the data collected by the beat.

output.console:
  pretty: true
# ---------------------------- Elasticsearch Output ----------------------------
# output.elasticsearch:
#   # Array of hosts to connect to.
#   hosts: ["localhost:9200"]

  # Protocol - either `http` (default) or `https`.
  #protocol: "https"

  # Authentication credentials - either API key or username/password.
  #api_key: "id:api_key"
  #username: "elastic"
  #password: "changeme"

# ------------------------------ Logstash Output -------------------------------
#output.logstash:
  # The Logstash hosts
  #hosts: ["localhost:5044"]

  # Optional SSL. By default is off.
  # List of root certificates for HTTPS server verifications
  #ssl.certificate_authorities: ["/etc/pki/root/ca.pem"]

  # Certificate for SSL client authentication
  #ssl.certificate: "/etc/pki/client/cert.pem"

  # Client Certificate Key
  #ssl.key: "/etc/pki/client/cert.key"

# ================================= Processors =================================
# processors:
#   - add_host_metadata:
#       when.not.contains.tags: forwarded
#   - add_cloud_metadata: ~
#   - add_docker_metadata: ~
#   - add_kubernetes_metadata: ~

# ================================== Logging ===================================

# Sets log level. The default log level is info.
# Available log levels are: error, warning, info, debug
#logging.level: debug

# At debug level, you can selectively enable logging only for some components.
# To enable all selectors, use ["*"]. Examples of other selectors are "beat",
# "publisher", "service".
#logging.selectors: ["*"]

# ============================= X-Pack Monitoring ==============================
# Filebeat can export internal metrics to a central Elasticsearch monitoring
# cluster.  This requires xpack monitoring to be enabled in Elasticsearch.  The
# reporting is disabled by default.

# Set to true to enable the monitoring reporter.
#monitoring.enabled: false

# Sets the UUID of the Elasticsearch cluster under which monitoring data for this
# Filebeat instance will appear in the Stack Monitoring UI. If output.elasticsearch
# is enabled, the UUID is derived from the Elasticsearch cluster referenced by output.elasticsearch.
#monitoring.cluster_uuid:

# Uncomment to send the metrics to Elasticsearch. Most settings from the
# Elasticsearch outputs are accepted here as well.
# Note that the settings should point to your Elasticsearch *monitoring* cluster.
# Any setting that is not set is automatically inherited from the Elasticsearch
# output configuration, so if you have the Elasticsearch output configured such
# that it is pointing to your Elasticsearch monitoring cluster, you can simply
# uncomment the following line.
#monitoring.elasticsearch:

# ============================== Instrumentation ===============================

# Instrumentation support for the filebeat.
#instrumentation:
    # Set to true to enable instrumentation of filebeat.
    #enabled: false

    # Environment in which filebeat is running on (eg: staging, production, etc.)
    #environment: ""

    # APM Server hosts to report instrumentation results to.
    #hosts:
    #  - http://localhost:8200

    # API Key for the APM Server(s).
    # If api_key is set then secret_token will be ignored.
    #api_key:

    # Secret token for the APM Server(s).
    #secret_token:


# ================================= Migration ==================================

# This allows to enable 6.7 migration aliases
#migration.6_to_7.enabled: true

