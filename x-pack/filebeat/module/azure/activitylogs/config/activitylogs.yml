type: kafka
hosts: {{ .namespace }}
topics: {{ .eventhub }}
group_id: {{ .consumer_group }}
yield_events_from_field: "records"

username: "$ConnectionString"
password: {{ .connection_string }}
ssl.enabled: true

processors:
- decode_json_fields:
    fields: ["message"]
    target: "azure.activitylogs"
