description: Pipeline for parsing sophos firewall logs (systemhealth pipeline)
processors:
#######################
## ECS Event Mapping ##
#######################
#TODO: Need to setup a different field naming convention, maybe "cpu.idle, cpu.system etc"
- set:
    field: event.kind
    value: event
- rename:
    field: sophos.xg.firewall.idle
    target_field: sophos.xg.firewall.idle_cpu
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.idle !=null"
- gsub:
    field: sophos.xg.firewall.idle_cpu
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "CPU"'
- convert:
    field: sophos.xg.firewall.idle_cpu
    target_field: sophos.xg.firewall.idle_cpu
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.idle_cpu != null"
- rename:
    field: sophos.xg.firewall.system
    target_field: sophos.xg.firewall.system_cpu
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.system !=null"
- gsub:
    field: sophos.xg.firewall.system_cpu
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "CPU"'
- convert:
    field: sophos.xg.firewall.system_cpu
    target_field: sophos.xg.firewall.system_cpu
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.system_cpu != null"
- rename:
    field: sophos.xg.firewall.user
    target_field: sophos.xg.firewall.user_cpu
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.user !=null"
- gsub:
    field: sophos.xg.firewall.user_cpu
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "CPU"'
- convert:
    field: sophos.xg.firewall.user_cpu
    target_field: sophos.xg.firewall.user_cpu
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.user_cpu != null"
- convert:
    field: sophos.xg.firewall.used
    target_field: sophos.xg.firewall.used
    type: integer
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.used != null"
- convert:
    field: sophos.xg.firewall.total_memory
    target_field: sophos.xg.firewall.total_memory
    type: integer
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.total_memory != null"
- convert:
    field: sophos.xg.firewall.free
    target_field: sophos.xg.firewall.free
    type: integer
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.free != null"
- gsub:
    field: sophos.xg.firewall.Configuration
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "Disk"'
- convert:
    field: sophos.xg.firewall.Configuration
    target_field: sophos.xg.firewall.configuration
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.Configuration != null"
- gsub:
    field: sophos.xg.firewall.Reports
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "Disk"'
- convert:
    field: sophos.xg.firewall.Reports
    target_field: sophos.xg.firewall.Reports
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.Reports != null"
- gsub:
    field: sophos.xg.firewall.Temp
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "Disk"'
- convert:
    field: sophos.xg.firewall.Temp
    target_field: sophos.xg.firewall.Temp
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.Temp != null"
- gsub:
    field: sophos.xg.firewall.Signature
    pattern: "(.{1}$)"
    replacement: ""
    if: 'ctx.sophos?.xg?.firewall?.log_component == "Disk"'
- convert:
    field: sophos.xg.firewall.Signature
    target_field: sophos.xg.firewall.Signature
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.Signature != null"
- convert:
    field: sophos.xg.firewall.users
    target_field: sophos.xg.firewall.users
    type: integer
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.users != null"
- convert:
    field: sophos.xg.firewall.transmittedkbits
    target_field: sophos.xg.firewall.transmittedkbits
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.transmittedkbits != null"
- convert:
    field: sophos.xg.firewall.receivedkbits
    target_field: sophos.xg.firewall.receivedkbits
    type: float
    ignore_failure: true
    ignore_missing: true
    if: "ctx.sophos?.xg?.firewall?.receivedkbits != null"

#############
## Cleanup ##
#############
on_failure:
- set:
    field: error.message
    value: '{{ _ingest.on_failure_message }}'
