module_version: 1.0

var:
  - name: paths
    default:
      - /var/lib/docker/containers/*/*-json.log
  - name: tags
    default: [coredns]

ingest_pipeline: ingest/pipeline.json
input: config/coredns.yml

requires.processors:
- name: geoip
