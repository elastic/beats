{
  "description": "Pipeline to parse s3 server access logs",
  "processors": [
    {
      "grok": {
        "field": "message",
        "patterns": [
          "%{BASE16NUM:s3_bucket_owner} %{HOSTNAME:s3_bucket} %{TIMESTAMP_ISO8601:timestamp} %{IP:s3_remote_ip} %{BASE16NUM:s3_requester} %{BASE16NUM:s3_request_id}"
        ]
      }
    }
  ]
}
