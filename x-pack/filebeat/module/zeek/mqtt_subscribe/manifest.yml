module_version: 1.0

var:
  - name: paths
    default:
      - /var/log/bro/current/mqtt_subscribe.log
    os.linux:
      - /var/log/bro/current/mqtt_subscribe.log
    os.darwin:
      - /usr/local/var/logs/current/mqtt_subscribe.log
  - name: tags
    default: [zeek.mqtt_subscribe]

ingest_pipeline: ingest/pipeline.yml
input: config/config.yml

requires.processors:
  - name: geoip
    plugin: ingest-geoip
