module_version: 1.0

var:
  - name: paths
    default:
      - /var/log/bro/current/mqtt_subscribe.log
    os.linux:
      - /var/log/bro/current/mqtt_subscribe.log
    os.darwin:
      - /usr/local/var/logs/current/mqtt_subscribe.log
  - name: tags
    default: [zeek.mqtt_subscribe]
  - name: community_id
    default: true

ingest_pipeline: ingest/pipeline.json
input: config/mqtt_subscribe.yml

requires.processors:
- name: geoip
  plugin: ingest-geoip
