module_version: 1.0

var:
  - name: paths
    default:
      - /var/log/bro/current/mqtt_connect.log
    os.linux:
      - /var/log/bro/current/mqtt_connect.log
    os.darwin:
      - /usr/local/var/logs/current/mqtt_connect.log
  - name: tags
    default: [zeek.mqtt_connect]

ingest_pipeline: ingest/pipeline.yml
input: config/config.yml

requires.processors:
  - name: geoip
    plugin: ingest-geoip
