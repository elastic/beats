{
  "description": "Pipeline for normalizing Zeek signatures.log",
  "processors": [
    {
      "set": {
        "field": "event.created",
        "value": "{{_ingest.timestamp}}"
      }
    },
    {
      "date": {
        "field": "zeek.signatures.ts",
        "formats": ["UNIX"]
      }
    },
    {
      "remove": {
        "field": "zeek.signatures.ts"
      }
    }
  ],
  "on_failure" : [{
    "set" : {
      "field" : "error.message",
      "value" : "{{ _ingest.on_failure_message }}"
    }
  }]
}
