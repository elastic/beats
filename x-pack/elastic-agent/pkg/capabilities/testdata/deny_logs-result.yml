outputs:
  default:
    type: elasticsearch
    hosts: [127.0.0.1:9200]
    service_token: test

inputs:
  - type: system/metrics
    data_stream.namespace: default
    use_output: default
    streams:
      - metricset: cpu
        data_stream.dataset: system.cpu
