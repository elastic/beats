- type: browser
  enabled: true
  id: my-monitor
  name: My Monitor
  source:
    inline:
      script:
        step("load homepage", async () => {
            await page.goto('https://blog.andrewvc.com');
        })
  schedule: "@every 1m"