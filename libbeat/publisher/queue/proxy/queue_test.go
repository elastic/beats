package proxyqueue

import (
	"testing"
)

func TestQueueStuff(t *testing.T) {
	// TODO: document and test the proxy loop's data invariants
	/*logger := logp.NewLogger("proxy-queue-tests")
	testQueue := NewQueue(logger, Settings{BatchSize: 2})*/
}
