############################# Mockbeat ######################################
mockbeat:

# No "name:" set — agent.name defaults to the OS hostname.

output.elasticsearch:
    hosts: ["localhost:9200"]
