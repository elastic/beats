queue_rules:
  - name: default
    conditions:
      - check-success=beats-ci/pr-merge
pull_request_rules:
  - name: self-assign PRs
    conditions:
      - -merged
      - -closed
      - "#assignee=0"
    actions:
      assign:
        add_users:
          - "{{ author }}"
