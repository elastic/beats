queue_rules:
  - name: default
    conditions:
      - check-success=beats-ci/pr-merge
pull_request_rules:
  - name: automatic merge for Dependabot pull requests
    actions:
      merge:
        method: merge
