# Kind can be one of:
# - breaking-change: a change to previously-documented behavior
# - deprecation: functionality that is being removed in a later release
# - bug-fix: fixes a problem in a previous version
# - enhancement: extends functionality but does not break or fix existing behavior
# - feature: new functionality
# - known-issue: problems that we are aware of in a given version
# - security: impacts on the security of a product or a user’s deployment.
# - upgrade: important information for someone upgrading from a prior version
# - other: does not fit into any of the other categories
kind: enhancement

# Change summary; a 80ish characters long description of the change.
summary: Make file scanning in the filestream input faster and lighter on memory

# Long description; in case the summary is not enough to describe the change
# this field accommodate a description without length limits.
description: >
  The filestream input now finds the files matching your "paths" by walking each
  directory only once and skipping excluded files as it goes, instead of first
  listing every match and applying "exclude_files" afterwards. When the paths
  match many files — for example a broad "**" pattern, or an "exclude_files" list
  that drops most of the matches — each scan uses roughly 60-90% less time and
  memory. The set of files that get ingested does not change.

# Affected component; usually one of "elastic-agent", "fleet-server", "filebeat", "metricbeat", "auditbeat", "all", etc.
component: filebeat

# PR URL; optional; the PR number that added the changeset.
# pr: https://github.com/owner/repo/1234

# Issue URL; optional; the GitHub issue related to this changeset (either closes or is part of).
issue: https://github.com/elastic/beats/issues/48686
