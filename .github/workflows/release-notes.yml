name: Generate release notes

on:
  workflow_dispatch:
    inputs:
      bc_commit_sha:
        description: 'Commit SHA for the build candidate'
        type: string
        required: true
      version:
        description: 'Version to be released (for example, `9.2.1`)'
        type: string
        required: true

jobs:
  build-release-notes:
    uses: colleenmcginnis/elastic-agent-changelog-tool/.github/workflows/generate-release-notes.yml@fix-cleanup-step
    with:
      product: Beats
      labels: docs,backport-active-9,forwardport-main,release,skip-changelog,Team:Docs
      branching_model: 'latest minor version'
      bc_commit_sha: ${{ inputs.bc_commit_sha }}
      version: ${{ inputs.version }}
      release_manager: ${{ github.triggering_actor }}
    permissions:
      contents: read
      id-token: write
