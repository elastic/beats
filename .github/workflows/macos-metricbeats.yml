name: build

env:
  GO111MODULE: off

on:
  pull_request:
    paths:
      - 'metricbeat/**'

jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.17.2'
    - uses: actions/checkout@v2
    - name: Run build
      run: cd metricbeat && mage build
    - name: Run test
      run: cd metricbeat && mage unitTest
