services:
  kafka:
    image: kafka-test:latest
    build:
      context: ./_meta
      args:
        KAFKA_VERSION: ${KAFKA_VERSION:-3.6.0}
    ports:
      - 9092
      - 8779
      - 8775
      - 8774
