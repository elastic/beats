version: "2.3"

services:
  kafka:
    image: metricbeat-kafka:${KAFKA_VERSION:-2.0.0}
    build:
      context: ./_meta
      args:
        KAFKA_VERSION: ${KAFKA_VERSION:-2.0.0}
    environment:
      # Needed by now
      KAFKA_ADVERTISED_HOST: "kafka:9092"
    ports:
    - 9092
