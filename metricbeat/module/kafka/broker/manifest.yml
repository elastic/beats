default: true
input:
  module: jolokia
  metricset: jmx
  defaults:
    namespace: "broker"
    jmx.mappings:
      - mbean: 'kafka.network:type=RequestChannel,name=RequestQueueSize'
        attributes:
          - attr: Value
            field: request.channel.queue.size
