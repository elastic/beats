- key: traefik
  title: "Traefik"
  description: >
    Traefik reverse proxy / load balancer metrics
  release: ga
  settings: ["ssl", "http"]
  fields:
    - name: traefik
      type: group
      description: >
        Traefik reverse proxy / load balancer metrics
      fields:
