- name: services
  type: group
  description: >
    `services` contains the status for windows services.
  fields:
    - name: status
      type: keyword
      description: >
        Which kind of service status to read. Possible values are
        `all`, `active` and `inactive`.
