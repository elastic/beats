- key: jolokia
  title: "Jolokia"
  description: >
    Jolokia module
  short_config: false
  release: ga
  settings: ["ssl", "http"]
  fields:
    - name: jolokia
      type: group
      description: >
          jolokia contains metrics exposed via jolokia agent
      fields:
        - name: agent.version
          type: keyword
          description: >
            version number of jolokia agent.
