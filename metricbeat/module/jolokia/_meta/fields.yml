- key: jolokia
  title: "Jolokia"
  description: >
    beta[]

    Jolokia module
  short_config: false
  fields:
    - name: jolokia
      type: group
      description: >
          jolokia contains metrics exposed via jolokia agent
      fields:
