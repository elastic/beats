- key: jolokia
  title: "Jolokia"
  description: >
    Jolokia module
  short_config: false
  release: beta
  fields:
    - name: jolokia
      type: group
      description: >
          jolokia contains metrics exposed via jolokia agent
      fields:
