- name: plusupstream
  type: group
  description: >
    `plusupstream` reads server upstream status from Nginx ngx_http_status_module.
  fields:
    - name: hostname
      type: keyword
      description: >
        Nginx hostname
