# yaml-language-server: $schema=https://raw.githubusercontent.com/buildkite/pipeline-schema/main/schema.json
name: "beats-xpack-metricbeat"

env:
  BEATS_PROJECT_NAME: "x-pack/metricbeat"

steps:

  - input: "Input Parameters"
    key: "force-run-stages"
    fields:
    - select: "Metricbeat - run_xpack_metricbeat"
      key: "run_xpack_metricbeat"
      options:
        - label: "True"
          value: "true"
        - label: "False"
          value: "false"
      default: "false"
    - select: "Metricbeat - run_xpack_metricbeat_macos_tests"
      key: "run_xpack_metricbeat_macos_tests"
      options:
        - label: "True"
          value: "true"
        - label: "False"
          value: "false"
      default: "false"
    - select: "Metricbeat - run_xpack_metricbeat_aws_tests"
      key: "run_xpack_metricbeat_aws_tests"
      options:
        - label: "True"
          value: "true"
        - label: "False"
          value: "false"
      default: "false"

    if: "build.source == 'ui'"

  - wait: ~
    if: "build.source == 'ui'"
    allow_dependency_failure: false

  - label: ":linux: Load dynamic x-pack metricbeat pipeline"
    key: "xpack-metricbeat-pipeline"
    command: ".buildkite/scripts/generate_xpack_metricbeat_pipeline.sh"
    notify:
      - github_commit_status:
          context: "${BEATS_PROJECT_NAME}: Load dynamic pipeline's steps"
