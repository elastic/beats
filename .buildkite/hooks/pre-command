#!/bin/bash

set -euo pipefail

source .buildkite/metricbeat/scripts/common.sh

echo "Golang version:"
version=$(cat .go-version)
GO_VERSION="${version}"
export GO_VERSION
echo "GO_VERSION: ${GO_VERSION}"
